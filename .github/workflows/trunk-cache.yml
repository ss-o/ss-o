---
name: ⭕ Trunk [cache]
on:
  push:
    branches: [main]
    tags: [v*.*.*]
    paths: [.trunk/trunk.yaml]
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions: read-all

jobs:
  cache_trunk:
    runs-on: ubuntu-latest
    permissions:
      actions: write      
    steps:
      - name: ✅ Checkout
        uses: actions/checkout@v3
      - name: ✨ Trunk Check
        uses: trunk-io/trunk-action@368a9c7cdd8132ed8df408bbefce3ab791084868
        with:
          check-mode: populate_cache_only
