---
name: ⭕ Trunk [cache]
on:
  push:
    branches: [main]
    tags: [v*.*.*]
    paths: [.trunk/trunk.yaml]
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions: read-all

jobs:
  cache:
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: ✅ Checkout
        uses: actions/checkout@v3
      - name: ✨ Trunk Check
        uses: trunk-io/trunk-action@d50681d780881d1896d2b5136ddeff2821950cec
        with:
          check-mode: populate_cache_only
