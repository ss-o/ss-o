---
name: ⭕ Trunk [cache]
on:
  push:
    branches: [main]
    tags: [v*.*.*]
    paths: [.trunk/trunk.yaml]
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions: read-all

jobs:
  cache:
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: ✅ Checkout
        uses: actions/checkout@v3
      - name: ✨ Trunk Check
        uses: trunk-io/trunk-action@8ffc7d856c517d640c6d387b96f5b66c63ed88ad
        with:
          check-mode: populate_cache_only
